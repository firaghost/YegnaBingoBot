{
  "version": 2,
  "framework": "nextjs",
  "ignoreCommand": "bash -c 'if git diff HEAD^ HEAD --quiet -- dashboard/; then echo \"No changes in dashboard\"; exit 1; else echo \"Changes detected in dashboard\"; exit 0; fi'"
}
