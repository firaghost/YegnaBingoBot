{
  "version": 2,
  "name": "yegna-bingo-bot",
  "builds": [
    {
      "src": "api/webhook.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/webhook",
      "dest": "/api/webhook.js"
    },
    {
      "src": "/(.*)",
      "dest": "/api/webhook.js"
    }
  ],
  "ignoreCommand": "bash -c 'if git diff HEAD^ HEAD --quiet -- bot/; then echo \"No changes in bot\"; exit 1; else echo \"Changes detected in bot\"; exit 0; fi'"
}
