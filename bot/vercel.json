{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs18.x"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "ignoreCommand": "bash -c 'git diff HEAD^ HEAD --quiet -- bot/ && exit 1 || exit 0'"
}
